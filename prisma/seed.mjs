// prisma/seed.mjs
/**
* 开发环境占位 seed 脚本：
* 不连接数据库，不插入任何数据，只打印一行日志然后正常退出（exit code 0）。
* 保证 `npx prisma migrate dev` / `npx prisma migrate reset` 不会因为 seed 挂掉。
*/
console.log("Seed placeholder: skipping database seeding. No records created.");